/**
 * Classification libellé → compte (alias historique du moteur hybride).
 */
export {
  categorizeTransaction,
  extractMemorableKeyword,
  type CategorySuggestion,
} from "@/lib/categorization-engine";
