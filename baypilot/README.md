# BayPilot

Copie améliorée du cockpit dropshipping eBay, **séparée** du clone `ebx/` qui tourne déjà sur le VPS.

- Opérateur DFY : `npm run ops` → http://127.0.0.1:3100  
- Cockpit (solo ou un client) : `npm start` → http://127.0.0.1:3101  
- Marche à suivre business + VPS : [`PLAYBOOK.md`](./PLAYBOOK.md)

Ne démarre pas ce dossier avec le nom PM2 `ebx`. Ne déploie pas par-dessus `/var/www/ebx`.
