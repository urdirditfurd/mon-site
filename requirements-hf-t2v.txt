# Dépendances pour le microservice HF Text-to-Video (server/hf_t2v_service.py)
# Installation : pip install -r requirements-hf-t2v.txt
#
# GPU NVIDIA recommandé (8+ GB VRAM pour wan-1.3b / ltx, 18+ GB pour wan-14b)
# CPU possible mais très lent (plusieurs heures par vidéo)

# Framework web
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.0.0

# HuggingFace / diffusers
diffusers>=0.32.0
transformers>=4.40.0
accelerate>=0.26.0
huggingface_hub>=0.30.0
sentencepiece>=0.1.99

# PyTorch — choisir la variante adaptée :
# CUDA 12.x (GPU NVIDIA) :
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# CPU seul (très lent) :
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# Apple Silicon (MPS) :
#   pip install torch torchvision torchaudio
torch>=2.1.0
torchvision>=0.16.0

# Export vidéo
imageio[ffmpeg]>=2.33.0
